# Stuart Wakefield 2016 Styles Loading

Usage:

```sh
bower install --save stuartwakefield2016-styles-loading
```

The structure of the loading fragment expected is as follows:

```html
<... class="loading">...</...>
```
 When finished loading add the class `"loaded"`.
